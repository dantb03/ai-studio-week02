# **AI 오케스트레이션 스튜디오(STS3001-01) 2주차 과제2**
1. 상품코드를 입력하면 즉시 재고 수량을 확인해야 하는 재고 관리 데이터 -> 딕셔너리<br>
데이터가 바뀌어야하므로 튜플은 탈락이다. 리스트나 셋은 가능할 듯 하지만 상품코드라는 key로써 적합한 데이터가 있기 때문에 딕셔너리가 가장 적합하다.<br>
```python
products = {"사과": 10, "배": 5, "수박": 3}
products["사과"] = -1
```
2. 이벤트 응모 고객 명단에서 중복 응모를 제거하고, 기존 회원 명단과의 교집합을 구해야 하는 상황 - > 셋(set)<br>
중복 응모와 교집합에 set이 가장 적합하다. 다른 data structure들은 따로 복잡한 코드로 중복을 거르거나 교집합을 만들어야 하지만 set은 그럴필요가 없다.<br>
```python
classroom1 = set(["다니엘", "샘", "브라이언"])
classroom2 = set(["톰", "에이미", "엘마"])
new_classroom = classroom1 + classroom2
```
3. 월별 매출액을 1월부터 12월까지 순서대로 저장하고 순회하는 데이터 -> 리스트<br>
1월 부터 12월이 key로 적합할수 있지만 다양한 월별 매출액이 필요해지면 딕셔너리로는 복잡해진다. 순서대로 저장되어 있기때문에 리스트로 만들어도 인덱스로 무슨 달인지는 유추할수 있다. 또한 한달마다 새로운 데이터가 추가되기 때문에 튜플도 탈락이다. 물로 set도 순서가 없기때문에 탈락이다.<br>
```python
income = []
income.append(10000) #1월
income.append(9500) #2월
```
4. 한 번 발급되면 절대 변경되어서는 안 되는 (위도, 경도) 매장 좌표 -> 튜플<br>
절대 변경되어서는 안되는 data structure으로는 튜플이 가장 적합하다. 튜플을 쓰지 않으면 후에 변경될 여지가 남아있다.<br>
```python
null_island = (0,0)
```
5. 1,000만 줄짜리 웹 서버 접속 로그 파일에서 특정 조건의 줄 수를 세는 작업 -> 튜플<br>
로그 파일의 특정 줄수를 세기 때문에 줄마다 나눠서 읽어올 것이다. 그렇기 때문에 리스트와 튜플을 써야 할것이다. 튜플이 리스트보다 적합한 이유는 읽어온 데이터가 변하면 안된다는 점과 튜플이 리스트보다 처리속도가 빠르기 때문이다. 리스트는 데이터가 변하거나 늘어나는 여지를 두기 때문에 메모리를 더 많이 차지하지만 리스트는 메모리를 더 효율적으로 사용하기 때문이다.<br>
